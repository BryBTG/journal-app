<!DOCTYPE html>
<html>
  <head>
    <title>BryBTG Journal App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>


    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="container w-200 m-auto">
    <%= render 'shared/header'%>
    <% if notice %>
      <%= render 'layouts/alerts'%>
    <% end%>

    <%= yield %>

    <footer class="pt-3 mt-4 pb-3 mb-3 text-muted border-top text-center fixed-bottom">
      BryBTG © 2022
    </footer>
    </div>
  </body>
</html>
